#Meteor.methods
#    clearListData: (refId, partOf, listName) ->
        #TODO some security must be added, not sure how, the nameing of deferred elements is unclear as well
#        Data.remove {refId: refId, partOf: partOf, listName: listName}